import { combineReducers } from "redux";

import currentUserReducer from "./currentUserReducer";
// import currentAlphaReducer from "./currentAlphaReducer";

export default combineReducers({
  currentUserReducer,
  //   currentAlphaReducer,
});
